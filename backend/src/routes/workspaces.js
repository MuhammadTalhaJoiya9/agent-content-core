const express = require('express');
const router = express.Router();

// Workspaces routes placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Workspaces routes' });
});

module.exports = router;